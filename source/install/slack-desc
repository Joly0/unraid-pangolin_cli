# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.

              |-----handy-ruler------------------------------------------------------|
pangolin_cli: pangolin_cli (Pangolin CLI machine client for Unraid)
pangolin_cli:
pangolin_cli: Installs the Pangolin CLI and adds a settings page to connect
pangolin_cli: this server to a Pangolin instance as a machine client, so that
pangolin_cli: private resources can be reached on this host.
pangolin_cli:
pangolin_cli: Homepage: https://github.com/fosrl/cli
pangolin_cli:
pangolin_cli:
pangolin_cli:
pangolin_cli:
